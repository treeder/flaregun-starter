{
  "name": "flaregun-starter",
  "type": "module",
  "version": "2.0.0",
  "main": "index.js",
  "scripts": {
    "start": "vite dev",
    "build": "vite build",
    "run": "vite --port 8787",
    "test:run": "vitest run",
    "test": "vitest run",
    "setup": "npx -y treeder/flaregun setup",
    "deploy": "npm run build && wrangler deploy",
    "deploy:preview": "npm run build && wrangler versions upload",
    "deploy:dev": "cross-env CLOUDFLARE_ENV=staging npm run build && cross-env CLOUDFLARE_ENV=staging wrangler deploy",
    "deploy:dev:preview": "cross-env CLOUDFLARE_ENV=staging npm run build && cross-env CLOUDFLARE_ENV=staging wrangler versions upload",
    "deploy:prod": "cross-env CLOUDFLARE_ENV=prod npm run build && cross-env CLOUDFLARE_ENV=prod wrangler deploy",
    "deploy:prod:preview": "cross-env CLOUDFLARE_ENV=prod npm run build && cross-env CLOUDFLARE_ENV=prod wrangler versions upload",
    "pushdev": "git push origin --force `git symbolic-ref --short HEAD`:dev",
    "format": "prettier --write ."
  },
  "author": "treeder",
  "license": "Apache-2.0",
  "description": "",
  "dependencies": {
    "api": "github:treeder/api#semver:^1",
    "console-logger": "github:treeder/console-logger#semver:^1",
    "flaregun": "github:treeder/flaregun#semver:^4",
    "migrations": "github:treeder/migrations#semver:^1",
    "nanoid": "^5.1.5",
    "once": "github:treeder/once#semver:^1",
    "passkeys": "github:treeder/passkeys#semver:^3",
    "rend": "github:treeder/rend#semver:^1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.3",
    "cross-env": "^7.0.3",
    "prettier": "^3.9.4",
    "vite": "^8.1.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.106.0"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "all",
    "printWidth": 120,
    "bracketSameLine": true
  }
}

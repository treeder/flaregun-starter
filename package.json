{
  "name": "flaregun-starter",
  "type": "module",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npm install && npx wrangler dev --env dev --live-reload",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx treeder/flaregun build",
    "setup": "npx treeder/flaregun setup"
  },
  "author": "treeder",
  "license": "Apache-2.0",
  "description": "",
  "dependencies": {
    "api": "github:treeder/api",
    "console-logger": "github:treeder/console-logger",
    "flaregun": "github:treeder/flaregun",
    "migrations": "github:treeder/migrations",
    "nanoid": "^5.1.5",
    "once": "github:treeder/once",
    "rend": "github:treeder/rend"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "tabWidth": 2,
    "trailingComma": "all",
    "printWidth": 120
  }
}
